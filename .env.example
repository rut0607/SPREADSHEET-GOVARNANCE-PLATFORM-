# This project has no single root .env — the server and client each load their
# own, from their own working directory:
#
#   server/.env.example  →  copy to server/.env
#   client/.env.example  →  copy to client/.env
#
# See docs/ENVIRONMENT_VARIABLES.md for a full reference of every variable.
